source 'https://rubygems.org'

gem 'rake'
gem 'motion-flow'#, github: 'HipByte/Flow' # FIXME: currently need to "gem install" or manually build the gem from source in order to compile the C code

group :ios do
  gem 'sugarcube', require: ['sugarcube-nsdate']
end

group :android do
  # ...
end
